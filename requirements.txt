# CPU-optimized dependencies for FitTürkAI RAG system
# Core ML libraries - CPU versions
torch>=2.0.0,<2.3.0  # Stable version for CPU
transformers>=4.35.0,<4.42.0  # Stable transformers
sentence-transformers>=2.2.2  # Lightweight embeddings
accelerate>=0.24.0  # For model loading optimization

# Optional for specific cases (comment out if causing issues)
# bitsandbytes  # Disabled for CPU - causes issues on Windows/CPU
# peft>=0.6.0  # For LoRA adapters - keep if using fine-tuned models

# Text processing
nltk>=3.8
regex>=2022.10.31

# Vector storage and search
faiss-cpu>=1.7.4  # CPU-only FAISS
numpy>=1.24.0,<2.0.0
scipy>=1.9.0

# PDF processing (if needed)
PyPDF2>=3.0.0
pypdf>=3.0.0

# Utilities
tqdm>=4.64.0
requests>=2.28.0
gdown>=4.7.0  # For Google Drive downloads

# Optional: Lightweight alternatives (uncomment if needed)
# datasets>=2.14.0  # For dataset handling
# tokenizers>=0.15.0  # Fast tokenization